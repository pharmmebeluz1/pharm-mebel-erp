const translations = {
  uz: {
    tagline: "MEBEL ISHLAB CHIQARISH BOSHQARUV TIZIMI", tashkent: "Toshkent", welcome: "dasturiga xush kelibsiz!", opening: "Dastur ochilmoqda...", enter: "Kirish",
    loginTitle: "Kirish", loginSubtitle: "Hisobingizga kiring", username: "Foydalanuvchi nomi", password: "Parol", remember: "Meni eslab qol", or: "yoki", smsLogin: "SMS orqali kirish",
    chooseRole: "Rolni tanlang", chooseRoleSub: "Iltimos, o‘zingizning rolingizni tanlang", admin: "Admin", management: "Boshqaruv", worker: "Ishchi", workshopEmployee: "Korxona xodimi",
    driver: "Shofyor", delivery: "Yetkazib berish", manager: "Menejer", clients: "Mijozlar bilan ish", constructor: "Konstruktor", designing: "Loyihalash", customer: "Mijoz", myContract: "Mening shartnomam",
    fillFields: "Login va parolni kiriting.", success: "Kirish muvaffaqiyatli.", smsSoon: "SMS orqali kirish keyingi bosqichda ulanadi.", roleSelected: "bo‘limi tanlandi.",
    fullContract: "To‘liq shartnoma", loadingContract: "Shartnoma ochilmoqda...", contractNumber: "Shartnoma raqami", orderCode: "Buyurtma kodi", contractStatus: "Holati",
    acceptContractText: "Shartnomaning barcha bandlarini o‘qidim va roziman.", printPdf: "Chop etish / PDF", confirmContract: "Shartnomani tasdiqlash", contractSavedNote: "Tasdiq sanasi va vaqti tizimda saqlanadi.",
    contractLoaded: "Shartnoma avtomatik ochildi.", contractConfirmed: "Shartnoma tasdiqlandi.", acceptFirst: "Avval shartnoma bilan tanishganingizni belgilang.", confirmed: "Tasdiqlangan", notConfirmed: "Tasdiqlanmagan",
    writeManager: "Menejerga yozish", supportShortcutText: "Buyurtma, chizma, to‘lov yoki o‘zgarish bo‘yicha xabar yuboring", supportCenter: "Murojaatlar markazi", directContact: "To‘g‘ridan-to‘g‘ri aloqa",
    managerHelp: "Menejer sizga yordam beradi", supportSubtitle: "Savolingiz buyurtma kodi bilan saqlanadi. Muhim masalani rahbarga yuborish mumkin.", orderInfo: "Buyurtma ma’lumoti", newRequest: "Yangi murojaat",
    noMessages: "Hali xabar yo‘q", firstMessageHint: "Mavzuni tanlang va birinchi xabaringizni yozing.", aiAssistant: "Mebel360 AI yordamchi", aiAssistantHint: "Xabaringizni darhol qabul qiladi va menejerga yetkazadi", topic: "Mavzu", subject: "Qisqa sarlavha", messagePlaceholder: "Xabaringizni yozing...", attachFile: "Fayl", send: "Yuborish",
    sendToDirector: "Rahbarga yuborish", escalateHint: "Masala menejer orqali hal bo‘lmasa foydalaning", allRequests: "Jami", newRequests: "Yangi", importantRequests: "Muhim", unreadRequests: "O‘qilmagan",
    customerRequests: "Mijoz murojaatlari", inbox: "Kiruvchi xabarlar", all: "Barchasi", closed: "Yopilgan", selectRequest: "Murojaatni tanlang", selectRequestHint: "Mijoz yozishmasi shu yerda ochiladi.",
    saveStatus: "Holatni saqlash", replyPlaceholder: "Mijozga javob yozing...", reply: "Javob berish", messageSent: "Xabar yuborildi.", newRequestReady: "Yangi murojaat yozishingiz mumkin."
  },
  ru: {
    tagline: "СИСТЕМА УПРАВЛЕНИЯ МЕБЕЛЬНЫМ ПРОИЗВОДСТВОМ", tashkent: "Ташкент", welcome: "Добро пожаловать в программу!", opening: "Программа запускается...", enter: "Войти",
    loginTitle: "Вход", loginSubtitle: "Войдите в свою учётную запись", username: "Имя пользователя", password: "Пароль", remember: "Запомнить меня", or: "или", smsLogin: "Войти через SMS",
    chooseRole: "Выберите роль", chooseRoleSub: "Пожалуйста, выберите свою роль", admin: "Админ", management: "Управление", worker: "Рабочий", workshopEmployee: "Сотрудник предприятия", driver: "Водитель", delivery: "Доставка",
    manager: "Менеджер", clients: "Работа с клиентами", constructor: "Конструктор", designing: "Проектирование", customer: "Клиент", myContract: "Мой договор", fillFields: "Введите логин и пароль.", success: "Вход выполнен успешно.",
    smsSoon: "Вход через SMS будет подключён на следующем этапе.", roleSelected: "— роль выбрана.", fullContract: "Полный договор", loadingContract: "Договор открывается...", contractNumber: "Номер договора", orderCode: "Код заказа", contractStatus: "Статус",
    acceptContractText: "Я прочитал(а) все пункты договора и согласен(на).", printPdf: "Печать / PDF", confirmContract: "Подтвердить договор", contractSavedNote: "Дата и время подтверждения сохраняются в системе.", contractLoaded: "Договор открыт автоматически.",
    contractConfirmed: "Договор подтверждён.", acceptFirst: "Сначала отметьте, что вы ознакомились с договором.", confirmed: "Подтверждено", notConfirmed: "Не подтверждено",
    writeManager: "Написать менеджеру", supportShortcutText: "Отправьте сообщение по заказу, чертежу, оплате или изменению", supportCenter: "Центр обращений", directContact: "Прямая связь", managerHelp: "Менеджер поможет вам",
    supportSubtitle: "Вопрос сохраняется вместе с кодом заказа. Важное обращение можно передать руководителю.", orderInfo: "Информация о заказе", newRequest: "Новое обращение", noMessages: "Сообщений пока нет", firstMessageHint: "Выберите тему и напишите первое сообщение.", aiAssistant: "Помощник Mebel360 AI", aiAssistantHint: "Сразу примет сообщение и передаст его менеджеру",
    topic: "Тема", subject: "Краткий заголовок", messagePlaceholder: "Напишите сообщение...", attachFile: "Файл", send: "Отправить", sendToDirector: "Передать руководителю", escalateHint: "Используйте, если вопрос не решён менеджером",
    allRequests: "Всего", newRequests: "Новые", importantRequests: "Важные", unreadRequests: "Непрочитанные", customerRequests: "Обращения клиентов", inbox: "Входящие", all: "Все", closed: "Закрытые",
    selectRequest: "Выберите обращение", selectRequestHint: "Переписка с клиентом откроется здесь.", saveStatus: "Сохранить статус", replyPlaceholder: "Ответьте клиенту...", reply: "Ответить", messageSent: "Сообщение отправлено.", newRequestReady: "Можно написать новое обращение."
  },
  en: {
    tagline: "FURNITURE PRODUCTION MANAGEMENT SYSTEM", tashkent: "Tashkent", welcome: "Welcome to the application!", opening: "The app is starting...", enter: "Sign in", loginTitle: "Sign in", loginSubtitle: "Access your account",
    username: "Username", password: "Password", remember: "Remember me", or: "or", smsLogin: "Sign in with SMS", chooseRole: "Choose a role", chooseRoleSub: "Please select your role", admin: "Admin", management: "Management",
    worker: "Worker", workshopEmployee: "Company employee", driver: "Driver", delivery: "Delivery", manager: "Manager", clients: "Client relations", constructor: "Designer", designing: "Design", customer: "Customer", myContract: "My contract",
    fillFields: "Enter your username and password.", success: "Signed in successfully.", smsSoon: "SMS sign-in will be connected in the next stage.", roleSelected: "role selected.", fullContract: "Full contract", loadingContract: "Opening the contract...", contractNumber: "Contract number",
    orderCode: "Order code", contractStatus: "Status", acceptContractText: "I have read and agree to all terms of the contract.", printPdf: "Print / PDF", confirmContract: "Confirm contract", contractSavedNote: "The confirmation date and time are stored in the system.",
    contractLoaded: "The contract opened automatically.", contractConfirmed: "Contract confirmed.", acceptFirst: "First confirm that you have read the contract.", confirmed: "Confirmed", notConfirmed: "Not confirmed",
    writeManager: "Message the manager", supportShortcutText: "Send a message about the order, drawing, payment, or a change", supportCenter: "Support center", directContact: "Direct contact", managerHelp: "Your manager is ready to help",
    supportSubtitle: "Your question is saved with the order code. Important matters can be escalated to the director.", orderInfo: "Order information", newRequest: "New request", noMessages: "No messages yet", firstMessageHint: "Choose a topic and write your first message.", aiAssistant: "Mebel360 AI assistant", aiAssistantHint: "Receives your message immediately and forwards it to the manager",
    topic: "Topic", subject: "Short subject", messagePlaceholder: "Write your message...", attachFile: "File", send: "Send", sendToDirector: "Escalate to director", escalateHint: "Use this when the manager could not resolve the issue",
    allRequests: "Total", newRequests: "New", importantRequests: "Important", unreadRequests: "Unread", customerRequests: "Customer requests", inbox: "Inbox", all: "All", closed: "Closed", selectRequest: "Select a request",
    selectRequestHint: "The customer conversation will open here.", saveStatus: "Save status", replyPlaceholder: "Reply to the customer...", reply: "Reply", messageSent: "Message sent.", newRequestReady: "You can write a new request."
  }
};

const languageNames = [
  { code: "uz", label: "O‘zbekcha" }, { code: "ru", label: "Русский" }, { code: "en", label: "English" }
];

let currentLanguage = localStorage.getItem("mebel360-language") || "uz";
let toastTimer;
let activeContract = null;
let activeCustomerTicketId = null;
let customerSupportData = null;
let currentStaffRole = null;
let activeStaffTicketId = null;
let staffInboxData = null;
let staffFilter = "all";

function createLanguageBars() {
  document.querySelectorAll("[data-language-bar]").forEach((bar) => {
    bar.innerHTML = languageNames.map(({ code, label }) => `<button type="button" class="language-button" data-language="${code}">${label}</button>`).join("");
  });
}

function applyLanguage(language) {
  currentLanguage = translations[language] ? language : "uz";
  localStorage.setItem("mebel360-language", currentLanguage);
  document.documentElement.lang = currentLanguage;
  document.querySelectorAll("[data-i18n]").forEach((element) => {
    const key = element.dataset.i18n;
    if (translations[currentLanguage][key]) element.textContent = translations[currentLanguage][key];
  });
  document.querySelectorAll("[data-i18n-placeholder]").forEach((element) => {
    const key = element.dataset.i18nPlaceholder;
    if (translations[currentLanguage][key]) element.placeholder = translations[currentLanguage][key];
  });
  document.querySelectorAll("[data-language]").forEach((button) => button.classList.toggle("active", button.dataset.language === currentLanguage));
  updateTashkentClock();
  if (activeContract) updateContractConfirmationUI(activeContract);
}

function showScreen(screenId) {
  document.querySelectorAll(".screen").forEach((screen) => screen.classList.remove("active"));
  const target = document.getElementById(screenId);
  document.body.classList.toggle("wide-app", screenId === "staffSupportScreen");
  if (target) {
    target.classList.add("active");
    window.scrollTo({ top: 0, behavior: "smooth" });
  }
}

function updateTashkentClock() {
  const clock = document.getElementById("liveClock");
  if (!clock) return;
  const now = new Date();
  const locale = currentLanguage === "ru" ? "ru-RU" : currentLanguage === "en" ? "en-GB" : "uz-UZ";
  const time = new Intl.DateTimeFormat("en-GB", { timeZone: "Asia/Tashkent", hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false }).format(now);
  const weekday = new Intl.DateTimeFormat(locale, { timeZone: "Asia/Tashkent", weekday: "long" }).format(now);
  const date = new Intl.DateTimeFormat(locale, { timeZone: "Asia/Tashkent", day: "2-digit", month: "long", year: "numeric" }).format(now);
  clock.textContent = time;
  document.getElementById("weekdayText").textContent = weekday.charAt(0).toUpperCase() + weekday.slice(1);
  document.getElementById("dateText").textContent = date;
}

function runProgress() {
  let progress = 0;
  const fill = document.getElementById("progressFill");
  const text = document.getElementById("progressText");
  const timer = setInterval(() => {
    const jump = progress < 55 ? 4 : progress < 85 ? 2 : 1;
    progress = Math.min(progress + jump, 100);
    fill.style.width = `${progress}%`;
    text.textContent = `${progress}%`;
    if (progress >= 100) clearInterval(timer);
  }, 70);
}

function showToast(message) {
  const toast = document.getElementById("toast");
  toast.textContent = message;
  toast.classList.add("show");
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => toast.classList.remove("show"), 3000);
}

function setTheme(dark) {
  document.body.classList.toggle("dark", dark);
  localStorage.setItem("mebel360-theme", dark ? "dark" : "light");
  document.querySelectorAll("#themeToggle, .theme-clone").forEach((button) => { button.textContent = dark ? "☾" : "☀"; });
}

function escapeHtml(value) {
  return String(value ?? "").replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" }[char]));
}

async function readJson(response) {
  const result = await response.json().catch(() => ({ ok: false, message: "Server javobini o‘qib bo‘lmadi." }));
  if (!response.ok || !result.ok) throw new Error(result.message || "Xatolik yuz berdi.");
  return result;
}

async function submitLogin(event) {
  event.preventDefault();
  const username = document.getElementById("username").value.trim();
  const password = document.getElementById("password").value.trim();
  if (!username || !password) { showToast(translations[currentLanguage].fillFields); return; }
  const button = event.submitter;
  if (button) button.disabled = true;
  try {
    const response = await fetch("/api/login", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ username, password, remember: document.getElementById("rememberMe").checked })
    });
    const result = await readJson(response);
    showToast(translations[currentLanguage].success);
    if (result.auto_contract || result.next_screen === "contractScreen") {
      setTimeout(() => { showScreen("contractScreen"); loadCustomerContract(); }, 350);
    } else {
      setTimeout(() => showScreen("roleScreen"), 350);
    }
  } catch (error) { showToast(error.message || translations[currentLanguage].fillFields); }
  finally { if (button) button.disabled = false; }
}

function valueAtPath(object, path) {
  return path.split(".").reduce((current, key) => current && current[key], object);
}

function fillContract(contractData) {
  activeContract = contractData;
  document.querySelectorAll("[data-contract]").forEach((element) => {
    const value = valueAtPath(contractData, element.dataset.contract);
    element.textContent = value ?? "—";
  });
  document.getElementById("contractHeaderNumber").textContent = contractData.contract.number;
  document.getElementById("summaryContractNumber").textContent = contractData.contract.number;
  document.getElementById("summaryOrderCode").textContent = contractData.order.code;
  document.getElementById("summaryContractStatus").textContent = contractData.contract.status;
  updateContractConfirmationUI(contractData);
}

function updateContractConfirmationUI(contractData) {
  const approved = Boolean(contractData.contract.confirmed_at);
  const status = document.getElementById("summaryContractStatus");
  const approval = document.getElementById("contractElectronicApproval");
  const checkbox = document.getElementById("contractAcceptCheckbox");
  const confirmButton = document.getElementById("confirmContractBtn");
  const acceptRow = document.getElementById("contractAcceptRow");
  if (approved) {
    const text = `${translations[currentLanguage].confirmed}: ${contractData.contract.confirmed_date || "—"}`;
    approval.textContent = text;
    status.textContent = text;
    status.classList.add("confirmed");
    checkbox.checked = true;
    checkbox.disabled = true;
    confirmButton.disabled = true;
    confirmButton.querySelector("[data-i18n]").textContent = translations[currentLanguage].confirmed;
    acceptRow.classList.add("accepted");
  } else {
    approval.textContent = translations[currentLanguage].notConfirmed;
    status.textContent = contractData.contract.status;
    status.classList.remove("confirmed");
    checkbox.disabled = false;
    confirmButton.disabled = false;
    confirmButton.querySelector("[data-i18n]").textContent = translations[currentLanguage].confirmContract;
    acceptRow.classList.remove("accepted");
  }
}

async function loadCustomerContract() {
  const loading = document.getElementById("contractLoading");
  const content = document.getElementById("contractContent");
  loading.hidden = false;
  content.hidden = true;
  try {
    const result = await readJson(await fetch("/api/customer/contract", { headers: { Accept: "application/json" } }));
    fillContract(result.data);
    loading.hidden = true;
    content.hidden = false;
    showToast(translations[currentLanguage].contractLoaded);
  } catch (error) {
    loading.innerHTML = `<strong>${escapeHtml(error.message)}</strong>`;
    showToast(error.message);
  }
}

async function confirmContract() {
  if (!activeContract) return;
  const checkbox = document.getElementById("contractAcceptCheckbox");
  if (!checkbox.checked) { showToast(translations[currentLanguage].acceptFirst); return; }
  const button = document.getElementById("confirmContractBtn");
  button.disabled = true;
  try {
    const result = await readJson(await fetch("/api/customer/contract/confirm", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ contract_id: activeContract.contract.id, accepted: true })
    }));
    activeContract.contract.confirmed_at = result.confirmed_at;
    activeContract.contract.confirmed_date = result.confirmed_date;
    activeContract.contract.status = "Mijoz tasdiqladi";
    updateContractConfirmationUI(activeContract);
    showToast(translations[currentLanguage].contractConfirmed);
  } catch (error) { button.disabled = false; showToast(error.message); }
}

async function selectRole(role, card) {
  document.querySelectorAll(".role-card").forEach((item) => item.classList.remove("featured"));
  card.classList.add("featured");
  card.disabled = true;
  try {
    const result = await readJson(await fetch("/api/select-role", {
      method: "POST",
      headers: { "Content-Type": "application/json", "Cache-Control": "no-cache" },
      body: JSON.stringify({ role }),
      cache: "no-store"
    }));
    const roleName = card.querySelector("strong").textContent;
    showToast(`${roleName} ${translations[currentLanguage].roleSelected}`);

    const nextScreen = result.next_screen;
    if (nextScreen === "contractScreen") {
      showScreen("contractScreen");
      await loadCustomerContract();
      return;
    }
    if (nextScreen === "staffSupportScreen") {
      currentStaffRole = role;
      showScreen("staffSupportScreen");
      await loadStaffInbox();
      return;
    }

    showToast(`${roleName} bo‘limi keyingi bosqichda ulanadi.`);
  } catch (error) {
    showToast(error.message || "Bo‘limni ochishda xatolik yuz berdi.");
  } finally {
    card.disabled = false;
  }
}

function messageHtml(message, mineRole) {
  const isSystem = message.sender_role === "system";
  const isAI = message.sender_role === "ai";
  const isMine = message.sender_role === mineRole || (mineRole === "staff" && ["manager", "admin"].includes(message.sender_role));
  const classes = `message-row${isMine ? " mine" : ""}${isSystem ? " system" : ""}${isAI ? " ai" : ""}`;
  const initial = isAI ? "✦" : escapeHtml((message.sender_name || "M").trim().charAt(0).toUpperCase());
  const attachment = message.attachment_url
    ? `<a class="message-attachment" href="${escapeHtml(message.attachment_url)}" target="_blank" rel="noopener">📎 <span>${escapeHtml(message.attachment_name || "Fayl")}</span></a>` : "";
  return `<div class="${classes}">${isSystem ? "" : `<span class="message-avatar">${initial}</span>`}<div class="message-bubble"><div class="message-meta"><strong>${escapeHtml(message.sender_name)}</strong><time>${escapeHtml(message.created_text)}</time></div>${message.body ? `<p>${escapeHtml(message.body)}</p>` : ""}${attachment}</div></div>`;
}

function renderMessageList(container, messages, mineRole) {
  if (!messages.length) {
    container.innerHTML = `<div class="empty-conversation"><span>✉</span><strong>${escapeHtml(translations[currentLanguage].noMessages)}</strong><p>${escapeHtml(translations[currentLanguage].firstMessageHint)}</p></div>`;
    return;
  }
  container.innerHTML = messages.map((message) => messageHtml(message, mineRole)).join("");
  requestAnimationFrame(() => { container.scrollTop = container.scrollHeight; });
}

function statusClasses(ticket) {
  if (!ticket) return "";
  if (ticket.escalated_to_admin) return " escalated";
  if (["Yopildi", "Hal qilindi"].includes(ticket.status)) return " closed";
  return "";
}

async function openCustomerSupport() {
  showScreen("supportScreen");
  await loadCustomerSupport(activeCustomerTicketId);
}

async function loadCustomerSupport(ticketId = null) {
  try {
    const query = ticketId ? `?ticket_id=${encodeURIComponent(ticketId)}` : "";
    const result = await readJson(await fetch(`/api/customer/support${query}`));
    customerSupportData = result.data;
    activeCustomerTicketId = result.data.active_ticket?.id || null;
    renderCustomerSupport(result.data);
  } catch (error) { showToast(error.message); }
}

function renderCustomerSupport(data) {
  document.getElementById("supportOrderCode").textContent = data.order.code;
  document.getElementById("supportOrderName").textContent = `${data.order.code} • ${data.order.product_name}`;
  document.getElementById("supportOrderStatus").textContent = data.order.status;
  const ticketList = document.getElementById("customerTicketList");
  ticketList.innerHTML = data.tickets.map((ticket) => `
    <button class="customer-ticket-chip${ticket.id === activeCustomerTicketId ? " active" : ""}${ticket.customer_unread ? " unread" : ""}" type="button" data-customer-ticket="${ticket.id}">
      <strong>${escapeHtml(ticket.topic)}</strong><small>${escapeHtml(ticket.status)} • ${escapeHtml(ticket.last_message_text)}</small>
    </button>`).join("");

  const ticket = data.active_ticket;
  const topicFields = document.getElementById("customerTopicFields");
  const status = document.getElementById("customerTicketStatus");
  const escalate = document.getElementById("customerEscalateBtn");
  if (ticket) {
    document.getElementById("customerConversationTopic").textContent = ticket.topic;
    document.getElementById("customerConversationTitle").textContent = ticket.subject || ticket.topic;
    status.hidden = false;
    status.textContent = ticket.status;
    status.className = `ticket-status${statusClasses(ticket)}`;
    topicFields.hidden = true;
    escalate.disabled = ticket.escalated_to_admin || ["Yopildi", "Hal qilindi"].includes(ticket.status);
  } else {
    document.getElementById("customerConversationTopic").textContent = translations[currentLanguage].newRequest;
    document.getElementById("customerConversationTitle").textContent = translations[currentLanguage].writeManager;
    status.hidden = true;
    topicFields.hidden = false;
    escalate.disabled = true;
  }
  renderMessageList(document.getElementById("customerMessageList"), data.messages || [], "customer");
}

function startNewCustomerRequest() {
  activeCustomerTicketId = null;
  if (customerSupportData) renderCustomerSupport({ ...customerSupportData, active_ticket: null, messages: [] });
  document.getElementById("customerSupportSubject").value = "";
  document.getElementById("customerMessageBody").value = "";
  showToast(translations[currentLanguage].newRequestReady);
}

async function sendCustomerMessage(event) {
  event.preventDefault();
  const form = event.currentTarget;
  const button = form.querySelector("button[type='submit']");
  const data = new FormData(form);
  if (activeCustomerTicketId) data.set("ticket_id", activeCustomerTicketId);
  button.disabled = true;
  try {
    const result = await readJson(await fetch("/api/customer/support/messages", { method: "POST", body: data }));
    form.querySelector("textarea").value = "";
    document.getElementById("customerAttachment").value = "";
    document.getElementById("customerAttachmentName").textContent = translations[currentLanguage].attachFile;
    activeCustomerTicketId = result.ticket_id;
    await loadCustomerSupport(activeCustomerTicketId);
    showToast(result.message || translations[currentLanguage].messageSent);
  } catch (error) { showToast(error.message); }
  finally { button.disabled = false; }
}

async function escalateCustomerTicket() {
  if (!activeCustomerTicketId) return;
  try {
    const result = await readJson(await fetch(`/api/customer/support/${activeCustomerTicketId}/escalate`, { method: "POST" }));
    await loadCustomerSupport(activeCustomerTicketId);
    showToast(result.message);
  } catch (error) { showToast(error.message); }
}

async function loadStaffInbox(ticketId = null) {
  try {
    const query = ticketId ? `?ticket_id=${encodeURIComponent(ticketId)}` : "";
    const result = await readJson(await fetch(`/api/staff/support/inbox${query}`));
    staffInboxData = result.data;
    currentStaffRole = result.data.role;
    activeStaffTicketId = result.data.active_ticket?.id || null;
    renderStaffInbox(result.data);
  } catch (error) { showToast(error.message); }
}

function ticketMatchesFilter(ticket) {
  if (staffFilter === "new") return ["Yangi", "Mijoz javob berdi", "Rahbarga yuborildi"].includes(ticket.status);
  if (staffFilter === "escalated") return ticket.escalated_to_admin;
  if (staffFilter === "closed") return ["Yopildi", "Hal qilindi"].includes(ticket.status);
  return true;
}

function renderStaffTicketList() {
  const list = document.getElementById("staffTicketList");
  const tickets = (staffInboxData?.tickets || []).filter(ticketMatchesFilter);
  if (!tickets.length) {
    list.innerHTML = `<div class="staff-empty-list">Bu bo‘limda murojaat yo‘q.</div>`;
    return;
  }
  list.innerHTML = tickets.map((ticket) => {
    const unread = currentStaffRole === "admin" ? ticket.admin_unread : ticket.manager_unread;
    const initial = escapeHtml(ticket.customer_name.trim().charAt(0).toUpperCase());
    return `<button class="staff-ticket-card${ticket.id === activeStaffTicketId ? " active" : ""}" type="button" data-staff-ticket="${ticket.id}">
      <span class="staff-ticket-avatar">${initial}</span>
      <span class="staff-ticket-main"><strong>${escapeHtml(ticket.customer_name)}</strong><span>${escapeHtml(ticket.order_code)} • ${escapeHtml(ticket.topic)}</span><small>${escapeHtml(ticket.last_body || ticket.subject || "Fayl yuborilgan")}</small></span>
      <span class="staff-ticket-side"><time>${escapeHtml(ticket.last_message_text)}</time>${unread ? '<i class="unread-dot"></i>' : ""}${ticket.escalated_to_admin ? '<b class="priority-flag">⚑</b>' : ""}</span>
    </button>`;
  }).join("");
}

function renderStaffInbox(data) {
  document.getElementById("staffRoleLabel").textContent = data.role === "admin" ? "Rahbar" : "Menejer";
  document.getElementById("staffInboxTitle").textContent = data.role === "admin" ? "Rahbar murojaatlar nazorati" : "Menejer murojaatlar kabineti";
  document.getElementById("metricAll").textContent = data.counts.all;
  document.getElementById("metricNew").textContent = data.counts.new;
  document.getElementById("metricEscalated").textContent = data.counts.escalated;
  document.getElementById("metricUnread").textContent = data.counts.unread;
  renderStaffTicketList();

  const ticket = data.active_ticket;
  const empty = document.getElementById("staffEmptyState");
  const content = document.getElementById("staffConversationContent");
  if (!ticket) {
    empty.hidden = false;
    content.hidden = true;
    return;
  }
  empty.hidden = true;
  content.hidden = false;
  document.getElementById("staffTicketOrder").textContent = `${ticket.order_code} • ${ticket.priority}`;
  document.getElementById("staffTicketCustomer").textContent = ticket.customer_name;
  document.getElementById("staffTicketTopic").textContent = `${ticket.topic} — ${ticket.subject || ticket.topic}`;
  const status = document.getElementById("staffTicketStatus");
  status.textContent = ticket.status;
  status.className = `ticket-status${statusClasses(ticket)}`;
  document.getElementById("staffStatusSelect").value = ["Rahbarga yuborildi", "Mijoz javob berdi"].includes(ticket.status) ? "Ko‘rib chiqilmoqda" : ticket.status;
  const escalate = document.getElementById("staffEscalateBtn");
  escalate.disabled = ticket.escalated_to_admin || currentStaffRole === "admin";
  escalate.hidden = currentStaffRole === "admin";
  renderMessageList(document.getElementById("staffMessageList"), data.messages || [], "staff");
}

async function sendStaffReply(event) {
  event.preventDefault();
  if (!activeStaffTicketId) return;
  const form = event.currentTarget;
  const button = form.querySelector("button[type='submit']");
  button.disabled = true;
  try {
    const result = await readJson(await fetch(`/api/staff/support/${activeStaffTicketId}/messages`, { method: "POST", body: new FormData(form) }));
    form.querySelector("textarea").value = "";
    document.getElementById("staffAttachment").value = "";
    document.getElementById("staffAttachmentName").textContent = translations[currentLanguage].attachFile;
    await loadStaffInbox(activeStaffTicketId);
    showToast(result.message);
  } catch (error) { showToast(error.message); }
  finally { button.disabled = false; }
}

async function saveStaffStatus() {
  if (!activeStaffTicketId) return;
  try {
    const result = await readJson(await fetch(`/api/staff/support/${activeStaffTicketId}/status`, {
      method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: document.getElementById("staffStatusSelect").value })
    }));
    await loadStaffInbox(activeStaffTicketId);
    showToast(result.message);
  } catch (error) { showToast(error.message); }
}

async function escalateStaffTicket() {
  if (!activeStaffTicketId) return;
  try {
    const result = await readJson(await fetch(`/api/staff/support/${activeStaffTicketId}/escalate`, { method: "POST" }));
    await loadStaffInbox(activeStaffTicketId);
    showToast(result.message);
  } catch (error) { showToast(error.message); }
}

createLanguageBars();
applyLanguage(currentLanguage);
setTheme(localStorage.getItem("mebel360-theme") === "dark");
updateTashkentClock();
setInterval(updateTashkentClock, 1000);
runProgress();

document.addEventListener("click", (event) => {
  const languageButton = event.target.closest("[data-language]");
  if (languageButton) applyLanguage(languageButton.dataset.language);
  const backButton = event.target.closest("[data-back]");
  if (backButton) showScreen(backButton.dataset.back);
  const roleCard = event.target.closest("[data-role]");
  if (roleCard) selectRole(roleCard.dataset.role, roleCard);
  const customerTicket = event.target.closest("[data-customer-ticket]");
  if (customerTicket) loadCustomerSupport(Number(customerTicket.dataset.customerTicket));
  const staffTicket = event.target.closest("[data-staff-ticket]");
  if (staffTicket) loadStaffInbox(Number(staffTicket.dataset.staffTicket));
  const filter = event.target.closest("[data-filter]");
  if (filter) {
    staffFilter = filter.dataset.filter;
    document.querySelectorAll("[data-filter]").forEach((button) => button.classList.toggle("active", button === filter));
    renderStaffTicketList();
  }
  if (event.target.closest("#themeToggle, .theme-clone")) setTheme(!document.body.classList.contains("dark"));
});

document.getElementById("openLoginBtn").addEventListener("click", () => showScreen("loginScreen"));
document.getElementById("loginForm").addEventListener("submit", submitLogin);
document.getElementById("smsBtn").addEventListener("click", () => showToast(translations[currentLanguage].smsSoon));
document.getElementById("passwordToggle").addEventListener("click", () => {
  const input = document.getElementById("password");
  input.type = input.type === "password" ? "text" : "password";
});
document.getElementById("printContractBtn").addEventListener("click", () => window.print());
document.getElementById("confirmContractBtn").addEventListener("click", confirmContract);
document.getElementById("openSupportBtn").addEventListener("click", openCustomerSupport);
document.getElementById("newSupportRequestBtn").addEventListener("click", startNewCustomerRequest);
document.getElementById("customerMessageForm").addEventListener("submit", sendCustomerMessage);
document.getElementById("customerEscalateBtn").addEventListener("click", escalateCustomerTicket);
document.getElementById("customerAttachment").addEventListener("change", (event) => {
  document.getElementById("customerAttachmentName").textContent = event.target.files[0]?.name || translations[currentLanguage].attachFile;
});
document.getElementById("staffReplyForm").addEventListener("submit", sendStaffReply);
document.getElementById("saveStaffStatusBtn").addEventListener("click", saveStaffStatus);
document.getElementById("staffEscalateBtn").addEventListener("click", escalateStaffTicket);
document.getElementById("refreshStaffInboxBtn").addEventListener("click", () => loadStaffInbox(activeStaffTicketId));
document.getElementById("staffAttachment").addEventListener("change", (event) => {
  document.getElementById("staffAttachmentName").textContent = event.target.files[0]?.name || translations[currentLanguage].attachFile;
});

// Dastur tez-tez yangilanayotgani uchun eski Service Worker va brauzer keshi tozalanadi.
// Bu Admin/Menejer tugmasi eski JavaScript bilan qolib ketishining oldini oladi.
if ("serviceWorker" in navigator) {
  window.addEventListener("load", async () => {
    try {
      const registrations = await navigator.serviceWorker.getRegistrations();
      await Promise.all(registrations.map((registration) => registration.unregister()));
    } catch (_) {}
    try {
      if ("caches" in window) {
        const keys = await caches.keys();
        await Promise.all(keys.map((key) => caches.delete(key)));
      }
    } catch (_) {}
  });
}
